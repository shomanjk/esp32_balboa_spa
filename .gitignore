.pio
.vscode
compile_commands.json
platformio_local.ini
.clang_complete
.gcc-flags.json

# macOS
.DS_Store

# Claude Code / local agent worktrees (never version)
.claude/

# Cursor IDE local rules and settings (never version)
.cursor/

# Local PlatformIO installer artifact (if downloaded into repo root)
get-platformio.py

src/config.h
emulator/node_modules
src/main.ino.cpp
/scripts/python

# Full flash dumps from scripts/backup_flash.sh
/backups/

# Local spa/gateway log exports (web portal JSON, telnet captures, etc.)
/logs/

# Local Mosquitto in docker/mosquitto — optional secrets if you add password/TLS later
docker/mosquitto/config/passwd
docker/mosquitto/certs/

# Local-only docs: bridge/diag run outputs, telnet captures, marketing drafts, review notes.
# Keep curated matrices/examples and public docs in docs/ (tracked).
/docs/local/
