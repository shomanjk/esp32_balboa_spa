.pio
.vscode

# macOS
.DS_Store

# Local PlatformIO installer artifact (if downloaded into repo root)
get-platformio.py

src/config.h
emulator/node_modules
src/main.ino.cpp
/scripts/python

# Full flash dumps from scripts/backup_flash.sh
/backups/

# Local Mosquitto in docker/mosquitto — optional secrets if you add password/TLS later
docker/mosquitto/config/passwd
docker/mosquitto/certs/

# Generated bridge diagnostics outputs (keep matrix/example inputs tracked)
docs/bridge-raw-*-live-run.json
docs/bridge-raw-live-run.json
docs/bridge-raw-live-*.json
docs/bridge-raw-*-dry-run.json
docs/bridge-raw-*-diff-*.json
docs/bridge-raw-last-run.json
