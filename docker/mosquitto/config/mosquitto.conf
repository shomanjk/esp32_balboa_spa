# Local dev broker only — anonymous auth, not for WAN exposure.
# ESP32 must use this machine's LAN IP (not 127.0.0.1).

listener 1883
protocol mqtt
allow_anonymous true

persistence false

log_dest stdout
log_type error
log_type warning
log_type notice
connection_messages true
